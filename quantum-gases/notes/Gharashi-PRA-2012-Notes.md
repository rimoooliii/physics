# Gharashi et al. (2012) PRA 学习笔记

> **标题**: Three s-wave-interacting fermions under anisotropic harmonic confinement: Dimensional crossover of energetics and virial coefficients  
> **期刊**: Physical Review A 86, 042702 (2012)  
> **阅读日期**: 2026-02-04  
> **阅读者**: rimo  
> **笔记整理**: Susie 🩷

---

## 1. 引言与核心问题

### 1.1 研究背景

在超冷原子物理中，**维度效应**对费米子系统的行为有深刻影响：

- **三维（3D）**: s波散射长度 $a^{3D}$ 为正时存在弱束缚态，为负时不存在
- **严格一维（1D）和二维（2D）**: s波束缚态**对所有**散射长度值都存在

**核心问题**: 当囚禁势从**球形**（各向同性）逐渐变为**雪茄形**（cigar-shaped, $\eta > 1$）或**薄饼形**（pancake-shaped, $\eta < 1$）时，三体系统的能谱如何演化？

### 1.2 纵横比（Aspect Ratio）

定义轴向和径向囚禁频率之比：

$$
\eta = \frac{\omega_\rho}{\omega_z}
$$

- **$\eta = 1$**: 球形囚禁（3D）
- **$\eta > 1$**: 雪茄形（cigar-shaped），轴向频率较小，系统被拉伸
- **$\eta < 1$**: 薄饼形（pancake-shaped），径向频率较小，系统被压扁

**能量单位**: 轴向谐振子能量 $E_z = \hbar\omega_z$  
**长度单位**: 轴向谐振子长度 $a_z = \sqrt{\frac{\hbar}{\mu\omega_z}}$

---

## 2. 理论框架

### 2.1 系统哈密顿量

三粒子哈密顿量（两个自旋向上，一个自旋向下）：

$$
\hat{H} = \sum_{j=1}^{3} \hat{H}_0(\mathbf{r}_j, M) + \hat{V}_{\text{int}}
$$

其中单粒子哈密顿量：

$$
\hat{H}_0(\mathbf{r}_j, M) = -\frac{\hbar^2}{2M}\nabla^2_{\mathbf{r}_j} + \frac{1}{2}M(\omega_z^2 z_j^2 + \omega_\rho^2 \rho_j^2)
$$

相互作用势（s波零程赝势）：

$$
\hat{V}_{\text{int}} = \hat{V}_{\text{ps}}^{3D}(\mathbf{r}_{31}) + \hat{V}_{\text{ps}}^{3D}(\mathbf{r}_{32})
$$

### 2.2 Jacobi 坐标

引入相对坐标和质心坐标：

$$
\mathbf{r} = \mathbf{r}_{31}, \quad \mathbf{R} = \sqrt{\frac{2}{3}}\left(\frac{\mathbf{r}_1 + \mathbf{r}_2}{2} - \mathbf{r}_3\right)
$$

相对哈密顿量：

$$
\hat{H}_{\text{rel}} = \hat{H}_{\text{rel},0} + \hat{V}_{\text{int}}
$$

其中未微扰部分：

$$
\hat{H}_{\text{rel},0} = \hat{H}_0(\mathbf{r}, \mu) + \hat{H}_0(\mathbf{R}, \mu)
$$

约化质量 $\mu = M/2$。

### 2.3 Green函数方法

利用Lippmann-Schwinger方程：

$$
|\Psi\rangle = -\hat{G}(E_{3b}) \hat{V}_{\text{int}} |\Psi\rangle
$$

其中Green函数：

$$
\hat{G}(E_{3b}; \mathbf{r}, \mathbf{R}; \mathbf{r}', \mathbf{R}') = \sum_{\lambda_1, \lambda_2} \frac{\phi_{\lambda_1}^*(\mathbf{r}') \phi_{\lambda_2}^*(\mathbf{R}') \phi_{\lambda_1}(\mathbf{r}) \phi_{\lambda_2}(\mathbf{R})}{E_{\lambda_1} + E_{\lambda_2} - E_{3b}}
$$

单粒子基态（柱坐标）：

$$
E_\lambda = \left(n_z + \frac{1}{2}\right)\hbar\omega_z + (2n_\rho + |m| + 1)\eta\hbar\omega_z
$$

其中 $\lambda = (n_z, n_\rho, m)$。

### 2.4 赝势的边界条件

零程赝势在 $r \to 0$ 时施加边界条件：

$$
\Psi(\mathbf{r}, \mathbf{R})|_{r \to 0} \approx \frac{f(\mathbf{R})}{(4\pi a_z)^{3/2}} \left(\frac{a_z}{r} - \frac{a_z}{a^{3D}}\right)
$$

这等价于Bethe-Peierls边界条件。

### 2.5 核心方程（矩阵本征方程）

通过展开和投影，得到隐式本征方程：

$$
\sum_{\lambda'} \left[ I_{\lambda,\lambda'}^{3D}(\tilde{\lambda}, \eta) - F^{3D}(\tilde{\lambda}, \eta) \delta_{\lambda,\lambda'} \right] f_{\lambda'} = \frac{a_z}{a^{3D}} f_\lambda
$$

其中：
- $I_{\lambda,\lambda'}^{3D}$: 几何积分（包含轴向和径向部分）
- $F^{3D}(\tilde{\lambda}, \eta)$: 与两体问题相关的函数
- $f_\lambda$: 展开系数
- $\tilde{\lambda}$: 与相互作用对相关的非整数量子数

**物理解释**: 当相互作用关闭时，$I_{\lambda,\lambda'}^{3D} = 0$，方程退化为相互作用对+旁观粒子的解。

---

## 3. 各图物理图像详解

### Fig. 1: 雪茄形能谱（$\eta = 2$, $M = 0$）

**图形描述**: 
- (a) 偶宇称 $z$-parity $= +1$
- (b) 奇宇称 $z$-parity $= -1$
- x轴: $a_z/a^{3D}$（散射长度的倒数，从BCS到BEC）
- y轴: $E_{3b}/E_z$（三体能量，以轴向谐振子能量为单位）

**物理图像**:

**A. 非相互作用极限**（$|a^{3D}|^{-1} \to \pm\infty$）:

当 $a^{3D} \to 0^-$（弱排斥）或 $a^{3D} \to 0^+$（弱吸引）时，系统接近非相互作用三谐振子：

$$
E_{3b} \to E_{\text{non-int}} = \left(n_z + \frac{1}{2}\right) + (2n_\rho + |M| + 1)\eta
$$

**B. Unitarity极限**（$a^{3D} \to \infty$, 即 $a_z/a^{3D} = 0$）:

散射长度发散，相互作用最强。此时存在**普适性**（universality）：

$$
E_{3b}^{(\text{unit})} = \xi \cdot E_{\text{ho}}
$$

其中 $\xi$ 是普适常数。

**C. 能级分类**:

1. **原子-二聚体态**（Atom-dimer states）:
   - 大正散射长度时能量为负
   - 对应于一个自由原子 + 一个束缚二聚体
   - 能量: $E \approx E_{\text{pair}} + E_{\text{atom}}$

2. **三原子态**（Atom-atom-atom states）:
   - 能量为正
   - 三个原子都处于未束缚状态

**D. 宇称效应**:

- 偶宇称（$z = +1$）和奇宇称（$z = -1$）的能谱结构不同
- 在unitarity附近，某些能级会发生**交叉或避免交叉**

---

### Fig. 2: Crossover曲线（$M = 0$，多种$\eta$值）

**图形描述**:
- 不同$\eta$值（2, 4, 6, 8, 10）的基态能量
- 能量已移位 $2E_\rho = 2\eta E_z$ 以便比较
- x轴: $a_z/a^{3D}$

**物理图像**:

**A. 维度效应**:

随着$\eta$增大（越来越像1D）：
- 能谱变得更加"扁平"
- BCS-BEC crossover的特征变得更加明显

**B. 宇称变化点**:

图中用星号标记的是宇称从 $z = -1$ 变为 $z = +1$ 的散射长度值。

在这些点，crossover曲线的导数不连续（在所示尺度上看不到）。

**C. 大$\eta$极限**:

当 $\eta \gg 1$ 时，系统行为趋近于**严格一维**:

$$
E_{3b}^{(3D)} \approx E_{3b}^{(1D)} + 2E_\rho
$$

这表示轴向运动与径向运动**解耦**。

---

### Fig. 3 & Fig. 4: 准1D极限验证（$\eta = 10$）

**图形描述**:
- (a) 完整3D计算的三体能谱
- (b) 与严格1D方程解的比较（使用重归一化1D散射长度）

**物理图像**:

**A. 重归一化散射长度**:

在强囚禁极限下，1D散射长度与3D散射长度的关系：

$$
a_{\text{ren}}^{1D} = -\frac{a_\rho^2}{a^{3D}} \left(1 - C \frac{a_\rho}{a^{3D}}\right)^{-1}
$$

其中 $a_\rho = a_z/\sqrt{\eta}$ 是径向谐振子长度。

**B. 比较结果**:

- 实心线: 完整3D计算
- 虚线: 严格1D方程解

两者差异极小（在unitarity附近最大偏差约0.2%），说明**准1D近似**在$\eta = 10$时已经非常准确。

**C. 物理意义**:

这验证了**维度渡越**（dimensional crossover）的图像：
- 当 $\eta$ 很大时，径向运动被"冻结"在基态
- 系统动力学主要由轴向（1D）运动主导

---

### Fig. 5: 薄饼形能谱（$\eta = 1/2$）

**图形描述**:
- (a) $M = 0$, $z = +1$
- (b) $M = \pm 1$, $z = +1$
- x轴: $a_\rho/a^{3D}$（使用径向长度单位）
- y轴: $E_{3b}/E_\rho$（以径向谐振子能量为单位）

**物理图像**:

**A. 坐标交换**:

与雪茄形相反，薄饼形中：
- 径向是"软"方向（低频）
- 轴向是"紧"方向（高频）

因此使用 $E_\rho = \eta E_z$ 和 $a_\rho = a_z\sqrt{\eta}$ 作为单位更自然。

**B. 准2D行为**:

当 $\eta \ll 1$ 时，系统趋近于**严格二维**：

$$
E_{3b}^{(3D)} \approx E_{3b}^{(2D)} + E_z/2
$$

**C. 角动量效应**:

对比(a)和(b)：
- $M = 0$: s波主导
- $M = \pm 1$: p波特征，能谱结构不同

---

## 4. 维里系数计算

### 4.1 维里展开

热力学势的维里展开：

$$
\frac{P}{k_B T} = \frac{1}{\lambda_T^3} \sum_{n=1}^{\infty} b_n z^n
$$

其中:
- $z = e^{\beta\mu}$: 逸度
- $\lambda_T = \sqrt{\frac{2\pi\hbar^2}{Mk_B T}}$: 热德布罗意波长
- $b_n$: 第$n$阶维里系数

### 4.2 维里系数与能谱的关系

对于囚禁系统，维里系数可以通过**分立的能谱**计算：

$$
b_n = \sum_i e^{-\beta E_{n,i}}
$$

其中 $E_{n,i}$ 是$n$体系统的第$i$个本征能量。

具体地：

**第二维里系数**（两体）：

$$
b_2 = \sum_i e^{-\beta E_{2,i}} - \frac{1}{2}
$$

**第三维里系数**（三体）：

$$
b_3 = \sum_i e^{-\beta E_{3,i}} - 3b_2 - \frac{1}{3}
$$

### 4.3 Fig. 10-12: 维里系数的温度依赖

**关键发现**:

**A. 第二维里系数 $b_2$**:

- 高温极限（$T \to \infty$）: $b_2 \to 1/2$（与散射长度无关）
- 低温极限: 依赖于散射长度和囚禁几何

**B. 普适性**（Fig. 12）:

令人惊讶的结果：

对于实验相关温度，$b_2$ 对于**所有纵横比** $\eta$ 都近似等于球形囚禁（$\eta = 1$）的值，只要使用**平均谐振子单位**:

$$
E_{\text{ave}} = \frac{2E_\rho + E_z}{3}, \quad a_{\text{ave}} = \sqrt{\frac{\hbar}{\mu\omega_{\text{ave}}}}
$$

**物理解释**:

高激发态（对高温贡献最大）的能谱结构对不同$\eta$值相似，导致维里系数的普适行为。

**C. 对第三维里系数的推测**:

文章推测 $b_3$ 也表现出类似普适性（后续工作证实了这一猜测）。

---

## 5. 数学技巧详解

### 5.1 几何积分的计算

轴向部分：

$$
C_{n_z, n_z'}^{(k)} = \int_{-\infty}^{\infty} dz \, \phi_{n_z}(z) \phi_{n_z'}(-z/2) L_k^{(-1/2)}(3z^2/4a_z^2)
$$

利用Gauss-Hermite求积高效计算。

### 5.2 F函数的计算

$F^{3D}(\tilde{\lambda}, \eta)$ 包含超几何函数：

$$
F^{3D}(\tilde{\lambda}, \eta) = -2\frac{\Gamma(-\tilde{\lambda}/2)}{\Gamma(-\tilde{\lambda}/2 - 1/2)} + \frac{\Gamma(-\tilde{\lambda}/2)}{\Gamma(1/2 - \tilde{\lambda}/2)} \sum_{k=1}^{\eta-1} {}_2F_1(1, -\tilde{\lambda}/2, 1/2 - \tilde{\lambda}/2, e^{2\pi i k/\eta})
$$

对于薄饼形（$\eta < 1$），需要使用解析延拓：

$$
F^{3D}(\tilde{\lambda}, \eta) = \frac{1}{\sqrt{\eta}} F^{3D}(\tilde{\lambda}/\eta, 1/\eta)
$$

### 5.3 矩阵对角化

对于给定能量 $E_{3b}$，构建矩阵：

$$
M_{\lambda,\lambda'} = I_{\lambda,\lambda'}^{3D}(\tilde{\lambda}, \eta) - F^{3D}(\tilde{\lambda}, \eta)\delta_{\lambda,\lambda'}
$$

对角化得到本征值 $a_z/a^{3D}$，然后反解 $E_{3b}(a^{3D})$。

---

## 6. 物理意义与应用

### 6.1 维度渡越的微观理解

文章提供了**微观量子力学框架**来理解：

1. **如何从3D过渡到准1D/准2D**
2. **何时可以使用低维有效模型**
3. **低维模型的适用范围**

### 6.2 对多体物理的意义

三体能谱是理解**多体系统**的基础：

- **BCS-BEC crossover**: 三体行为反映了配对机制的变化
- **维里展开**: 少体能谱直接决定热力学性质
- **普适性**: 高温行为的几何不依赖性简化实验分析

### 6.3 实验相关性

现代超冷原子实验可以：
- 调节散射长度（Feshbach共振）
- 调节囚禁几何（光学阱形变）
- 测量维里系数（通过状态方程）

文章的预测可以直接在实验中验证。

---

## 7. 关键公式总结

| 物理量 | 公式 | 备注 |
|-------|------|------|
| 纵横比 | $\eta = \omega_\rho/\omega_z$ | 控制几何形状 |
| 单粒子能量 | $E_\lambda = (n_z+1/2) + (2n_\rho+|m|+1)\eta$ | 以 $E_z$ 为单位 |
| 核心本征方程 | $\sum_{\lambda'} [I_{\lambda,\lambda'} - F\delta_{\lambda,\lambda'}] f_{\lambda'} = \frac{a_z}{a^{3D}} f_\lambda$ | 隐式方程 |
| 维里系数 | $b_n = \sum_i e^{-\beta E_{n,i}}$ | 从能谱计算 |
| 高温极限 | $b_2 \to 1/2$, $b_3 \to 3^{-5/2}$ | 普适值 |
| 准1D条件 | $\eta \gg 1$ | 径向运动冻结 |
| 准2D条件 | $\eta \ll 1$ | 轴向运动冻结 |

---

## 8. 思考问题

1. **为什么严格1D和2D对所有散射长度都存在束缚态，而3D只在正散射长度时存在？**

2. **Fig. 2 中宇称变化点的物理意义是什么？为什么在 crossover 曲线上会出现宇称变化？**

3. **如何理解维里系数在高温极限下与几何形状无关的普适性？这对实验测量有什么实际意义？**

4. **文章中提到的 $F^{3D}$ 函数在 $\eta < 1$ 时需要解析延拓，这反映了什么物理？**

5. **如果你要设计一个实验来验证 Fig. 12 的普适性，你会如何调节温度和囚禁几何？**

---

## 9. 与当前研究的联系

你正在编写的 MATLAB 代码正是在实现这篇文章的**数值方法**。理解这些物理图像有助于：

1. **调试代码**: 知道预期的结果形状（Fig. 1-5）
2. **选择参数**: 理解为什么需要大的 $j_{\max}$ 和 $k_{\max}$
3. **解释结果**: 能从数值输出中提取物理意义
4. **扩展工作**: 可以尝试计算维里系数（Fig. 10-12）

---

## 参考文献

- Gharashi, S. E., Daily, K. M., & Blume, D. (2012). "Three s-wave-interacting fermions under anisotropic harmonic confinement." *Physical Review A*, 86(4), 042702.
- Bloch, I., Dalibard, J., & Zwerger, W. (2008). "Many-body physics with ultracold gases." *Reviews of Modern Physics*, 80(3), 885.
- Petrov, D. S., Salomon, C., & Shlyapnikov, G. V. (2004). "Weakly bound dimers of fermionic atoms." *Physical Review Letters*, 93(9), 090404.

---

*笔记完成于 2026-02-04*  
*如需深入讨论任何部分，继续交流 🩷*
