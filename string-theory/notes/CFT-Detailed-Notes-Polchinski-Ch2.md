# 弦论中共形场论详解 (CFT in String Theory)

> **来源**: Polchinski, "String Theory Vol. 1: An Introduction to the Bosonic String", Chapter 2  
> **主题**: 共形场论基础 - OPE、Virasoro代数、顶点算符  
> **整理**: Susie 🩷  
> **日期**: 2026-02-17

---

## 目录

1. [引言与基本设定](#1-引言与基本设定)
2. [二维无质量标量场](#2-二维无质量标量场)
3. [算符乘积展开 (OPE)](#3-算符乘积展开-ope)
4. [Ward恒等式与Noether定理](#4-ward恒等式与noether定理)
5. [共形不变性](#5-共形不变性)
6. [Virasoro代数](#6-virasoro代数)
7. [顶点算符](#7-顶点算符)
8. [总结与物理图像](#8-总结与物理图像)

---

## 1. 引言与基本设定

### 1.1 本章目标

本章发展世界面量子场论中的必要思想和技术，包括：
- **算符乘积展开 (OPE)**
- **共形不变性**
- **Virasoro代数**
- **顶点算符**

**重点**: 二维平坦空间中的共形不变场论——这是固定弦世界面局域对称性后剩下的理论。

### 1.2 欧几里得世界面

虽然物理弦用**洛伦兹签名**度规描述，但计算通常在**欧几里得世界面**上进行。

**Wick旋转**: $\sigma^2 \rightarrow i\sigma^0$

**原因**: 
- 大多数弦计算在欧几里得世界面上进行
- 平坦度规下，欧几里得和闵可夫斯基振幅通过标准解析延拓联系

---

## 2. 二维无质量标量场

### 2.1 作用量与复坐标

考虑D个自由标量场 $X^\mu(\sigma^1, \sigma^2)$，作用量为：

$$\boxed{S = \frac{1}{4\pi\alpha'} \int d^2\sigma \left(\partial_1 X^\mu \partial_1 X^\mu + \partial_2 X^\mu \partial_2 X^\mu\right)}$$

这是**Polyakov作用量**的平坦欧几里得版本。

**引入复坐标**:

$$\boxed{z = \sigma^1 + i\sigma^2, \quad \bar{z} = \sigma^1 - i\sigma^2}$$

**导数变换**:
$$\partial_z = \frac{1}{2}(\partial_1 - i\partial_2), \quad \partial_{\bar{z}} = \frac{1}{2}(\partial_1 + i\partial_2)$$

性质：$\partial_z z = 1, \quad \partial_z \bar{z} = 0$

### 2.2 度规约定

**复指标升降**:
$$g_{z\bar{z}} = g_{\bar{z}z} = \frac{1}{2}, \quad g_{zz} = g_{\bar{z}\bar{z}} = 0$$
$$g^{z\bar{z}} = g^{\bar{z}z} = 2, \quad g^{zz} = g^{\bar{z}\bar{z}} = 0$$

**重要公式**:
- $d^2z = 2d\sigma^1 d\sigma^2$ （雅可比行列式因子2）
- $\int d^2z \delta^2(z, \bar{z}) = 1$

### 2.3 作用量与运动方程

在复坐标下，作用量简化为：

$$\boxed{S = \frac{1}{2\pi\alpha'} \int d^2z \partial X^\mu \bar{\partial} X^\mu}$$

**经典运动方程**:
$$\boxed{\partial\bar{\partial} X^\mu(z, \bar{z}) = 0}$$

**物理诠释**:
- $\partial X^\mu$ 是**全纯函数**（holomorphic）
- $\bar{\partial} X^\mu$ 是**反全纯函数**（antiholomorphic）

**术语**:
- 全纯 = 左移（left-moving）
- 反全纯 = 右移（right-moving）

### 2.4 传播子与格林函数

**基本结果**: 通过路径积分可得

$$\langle X^\mu(z_1, \bar{z}_1) X^\nu(z_2, \bar{z}_2) \rangle = -\frac{\alpha'}{2} \eta^{\mu\nu} \ln|z_{12}|^2$$

其中 $z_{12} = z_1 - z_2$。

**算符方程**（Ehrenfest定理）:
$$\partial\bar{\partial} X^\mu(z, \bar{z}) = 0$$

**格林函数恒等式**:
$$\partial\bar{\partial} \ln|z|^2 = 2\pi\delta^2(z, \bar{z})$$

---

## 3. 算符乘积展开 (OPE)

### 3.1 OPE的基本思想

**核心对象**: 路径积分中局域算符乘积的期望值

$$\langle A_{i_1}(z_1, \bar{z}_1) A_{i_2}(z_2, \bar{z}_2) \cdots A_{i_n}(z_n, \bar{z}_n) \rangle$$

**OPE表述**: 两个靠近的局域算符可以用局域算符的和来近似：

$$\boxed{A_i(\sigma_1) A_j(\sigma_2) = \sum_k c^k_{ij}(\sigma_1 - \sigma_2) A_k(\sigma_2)}$$

**算符意义**: 在一般期望值内成立（只要$\sigma_1$和$\sigma_2$的距离比其他算符近）

**类比**: 类似于泰勒级数，但系数函数可以是奇异的

### 3.2 自由标量场的OPE

对于$X^\mu$理论，利用全纯函数理论：

**基本OPE**:

$$\boxed{X^\mu(z_1, \bar{z}_1) X^\nu(z_2, \bar{z}_2) = -\frac{\alpha'}{2}\eta^{\mu\nu} \ln|z_{12}|^2 + :X^\nu X^\mu(z_2, \bar{z}_2): + \sum_{k=1}^\infty \frac{1}{k!}(z_{12})^k :X^\nu \partial^k X^\mu: + (z_{12})^k :X^\nu \bar{\partial}^k X^\mu:}$$

**简化记号**: 常用$\sim$表示"相等至非奇异项"

**收敛性**: 在共形不变场论中，OPE实际上是**收敛的**（不只是渐近展开）

### 3.3 正规序 (Normal Ordering)

**定义**: 递归定义正规序乘积

$$:X^{\mu_1}(z_1, \bar{z}_1) \cdots X^{\mu_n}(z_n, \bar{z}_n): = X^{\mu_1}(z_1, \bar{z}_1) \cdots X^{\mu_n}(z_n, \bar{z}_n) + \text{(减缩项)}$$

**减缩规则**: 对所有选择一对、两对或更多对场的方式求和，每对用$\frac{\alpha'}{2}\eta^{\mu_i\mu_j}\ln|z_{ij}|^2$替代。

**紧凑表达式**:
$$:F: = \exp\left(\frac{\alpha'}{4} \int d^2z_1 d^2z_2 \ln|z_{12}|^2 \frac{\delta}{\delta X^\mu(z_1)} \frac{\delta}{\delta X_\mu(z_2)}\right) F$$

** Wick定理**:
$$F = :F: + \text{(缩并)}$$

### 3.4 导数的OPE

从基本OPE可导出：

$$\boxed{\partial X^\mu(z_1) \partial X^\nu(z_2) \sim -\frac{\alpha'}{(z_1-z_2)^2}\eta^{\mu\nu}}$$

$$\boxed{\bar{\partial} X^\mu(\bar{z}_1) \bar{\partial} X^\nu(\bar{z}_2) \sim -\frac{\alpha'}{(\bar{z}_1-\bar{z}_2)^2}\eta^{\mu\nu}}$$

**混合导数**:
$$\partial X^\mu(z_1) \bar{\partial} X^\nu(\bar{z}_2) \sim 0$$

（正则的，无奇异性）

---

## 4. Ward恒等式与Noether定理

### 4.1 对称性与守恒流

对于对称变换 $\delta X^\mu = -i\epsilon j^\mu$（$\epsilon$为无穷小常数），存在守恒流 $j_a$。

**Ward恒等式**: 对于插入算符$A$，有

$$\sum_{k=1}^n \langle A_1 \cdots \delta A_k \cdots A_n \rangle = \frac{1}{2\pi i} \oint_C \left(dz \langle j_z A_1 \cdots A_n \rangle - d\bar{z} \langle j_{\bar{z}} A_1 \cdots A_n \rangle\right)$$

其中$C$包围所有算符的位置。

### 4.2 共形对称性的流

对于共形变换 $\delta X^\mu = -v^a \partial_a X^\mu$，Noether流为：

$$j(z) = iv(z) T(z), \quad \bar{j}(\bar{z}) = i\bar{v}(\bar{z}) \bar{T}(\bar{z})$$

其中**能量-动量张量**：

$$\boxed{T(z) = -\frac{1}{\alpha'} :\partial X^\mu \partial X_\mu:}$$

$$\boxed{\bar{T}(\bar{z}) = -\frac{1}{\alpha'} :\bar{\partial} X^\mu \bar{\partial} X_\mu:}$$

**注意**: $T$是$\partial X$的正规序乘积。

---

## 5. 共形不变性

### 5.1 共形变换的定义

**一般共形变换**:
$$z' = f(z), \quad \bar{z}' = \bar{f}(\bar{z})$$

在$f$和$f^{-1}$全纯的地方是保角的（保持角度）。

### 5.2 初级场 (Primary Fields)

**定义**: 权重为$(h, \tilde{h})$的初级场在共形变换$z \rightarrow z'$下变换为：

$$\boxed{\mathcal{O}'(z', \bar{z}') = (\partial_z z')^{-h} (\partial_{\bar{z}} \bar{z}')^{-\tilde{h}} \mathcal{O}(z, \bar{z})}$$

**无穷小形式**: 对于$z \rightarrow z + v(z)$，

$$\delta \mathcal{O}(z, \bar{z}) = -h v(z) \partial \mathcal{O}(z, \bar{z}) - \mathcal{O}(z, \bar{z}) \partial v(z) - (h \rightarrow \tilde{h}, z \rightarrow \bar{z})$$

### 5.3 TT OPE与中心荷

**能量-动量张量的OPE**：

$$\boxed{T(z_1) T(z_2) \sim \frac{c/2}{(z_1-z_2)^4} + \frac{2T(z_2)}{(z_1-z_2)^2} + \frac{\partial T(z_2)}{z_1-z_2}}$$

其中$c$是**中心荷**（central charge）。

对于D个自由标量场：$\boxed{c = D}$

**有限变换**: 
$$T'(z') = (\partial_z z')^{-2} \left[T(z) - \frac{c}{12}\{z', z\}\right]$$

其中$\{z', z\}$是Schwarzian导数。

### 5.4 共形权重的例子

| 算符 | 权重$(h, \tilde{h})$ | 类型 |
|------|---------------------|------|
| $\partial X^\mu$ | $(1, 0)$ | 全纯张量 |
| $\bar{\partial} X^\mu$ | $(0, 1)$ | 反全纯张量 |
| $:e^{ik\cdot X}:$ | $(\alpha'k^2/4, \alpha'k^2/4)$ | 标量 |

---

## 6. Virasoro代数

### 6.1 坐标选择与模式展开

**两种自然坐标选择**:

1. **w坐标**: $w = \sigma^1 + i\sigma^2$，$w \sim w + 2\pi$
   - 时间对应$\text{Im}\, w$的平移

2. **z坐标**: $z = \exp(-iw) = \exp(-i\sigma^1 + \sigma^2)$
   - 时间是径向的，原点是无限过去

**关系**: 两者通过共形变换联系

### 6.2 Laurent展开与Virasoro生成元

对于全纯或反全纯算符，可做Laurent展开：

$$T_{zz}(z) = \sum_{m=-\infty}^{\infty} \frac{L_m}{z^{m+2}}$$

**Virasoro生成元**（围道积分定义）：

$$\boxed{L_m = \oint_C \frac{dz}{2\pi i} z^{m+1} T(z)}$$

其中$C$是绕原点逆时针的任意围道。

### 6.3 模式展开的对偶形式

在$w$坐标系中（$\sigma^2 = 0$时），

$$T_{ww}(w) = -\sum_{m=-\infty}^{\infty} e^{im\sigma^1 - m\sigma^2} T_m$$

其中
$$T_m = L_m - \delta_{m,0}\frac{c}{24}$$

**Hamilton量**:
$$H = \int_0^{2\pi} \frac{d\sigma^1}{2\pi} T_{22} = L_0 + \tilde{L}_0 - \frac{c + \tilde{c}}{24}$$

### 6.4 Virasoro代数

**基本对易子**: OPE决定电荷的代数。对于围道积分定义的电荷，

$$[Q_1, Q_2] = \oint_{C_2} \frac{dz_2}{2\pi i} \oint_{C_1(z_2)} \frac{dz_1}{2\pi i} j_1(z_1) j_2(z_2)$$

其中$C_1$在$C_2$内部。

**应用到Virasoro生成元**：

$$\boxed{[L_m, L_n] = (m-n)L_{m+n} + \frac{c}{12}(m^3 - m)\delta_{m+n,0}}$$

**关键特性**:
- 当$m = 0, \pm 1$时，中心荷项消失
- $L_0$生成时间平移（径向缩放）
- $L_{\pm 1}$生成特殊的共形变换

### 6.5 开弦的Virasoro代数

对于开弦，$w$的范围是$0 \leq \sigma^1 \leq \pi$，有边界条件。共形生成元变为：

$$L_m = \oint \frac{dw}{2\pi i} v^m(w) T(w)$$

边界条件要求$v$在边界处切向。

---

## 7. 顶点算符

### 7.1 顶点算符对应态

**基本思想**: 世界面上的每个局域算符对应Hilbert空间中的一个态。

**对应关系**: 在半无限圆柱$w$坐标中，初始态$|A\rangle$与局域算符$A$的关系通过围道积分建立。

### 7.2  tachyon顶点算符

**状态**: $|0; k\rangle$，动量为$k^\mu$的基态

**对应顶点算符**：
$$\boxed{V_k = :e^{ik\cdot X}:}$$

**权重**: $(h, \tilde{h}) = (\alpha'k^2/4, \alpha'k^2/4)$

**共形不变性要求**: $h = \tilde{h} = 1$，即$\alpha'k^2 = 4$

这给出快子质量壳条件：$m^2 = -k^2 = -4/\alpha'$

### 7.3 矢量顶点算符

**极化矢量**: $\zeta_\mu(k)$，满足$k^\mu \zeta_\mu = 0$（横向）

**顶点算符**：
$$\boxed{V_{\zeta} = \zeta_\mu :\partial X^\mu e^{ik\cdot X}:}$$

**权重**: $h = 1 + \alpha'k^2/4$

**无质量条件**: $\alpha'k^2 = 0$，即$k^2 = 0$

### 7.4 物理态条件

**一般条件**: 
- $L_0 |\psi\rangle = \tilde{L}_0 |\psi\rangle = 1$（质量壳条件）
- $L_n |\psi\rangle = 0$（$n > 0$）（正频模式湮灭）

**异常抵消**: 当$c = 26$时，保证理论的一致性。

---

## 8. 总结与物理图像

### 8.1 关键公式汇总

| 概念 | 公式 |
|------|------|
| **复坐标** | $z = \sigma^1 + i\sigma^2$ |
| **作用量** | $S = \frac{1}{2\pi\alpha'} \int d^2z \partial X^\mu \bar{\partial} X_\mu$ |
| **基本OPE** | $X^\mu(z_1)X^\nu(z_2) \sim -\frac{\alpha'}{2}\eta^{\mu\nu}\ln|z_{12}|^2$ |
| **应力张量** | $T(z) = -\frac{1}{\alpha'}:\partial X^\mu \partial X_\mu:$ |
| **Virasoro代数** | $[L_m, L_n] = (m-n)L_{m+n} + \frac{c}{12}(m^3-m)\delta_{m+n,0}$ |
| **快子顶点** | $V_k = :e^{ik\cdot X}:$ |
| **临界维数** | $c = D = 26$ |

### 8.2 物理图像

**CFT在弦论中的角色**:
1. **世界面理论**: 弦的二维世界面是一个共形场论
2. **态-算符对应**: 弦的量子态 ↔ 世界面上的局域算符
3. **散射振幅**: 通过顶点算符的关联函数计算
4. **约束条件**: Virasoro约束对应物理态条件

**为什么$c=26$**:
- 保证异常抵消（Weyl反常）
- 确保Lorentz不变性
- 是协变量子化的必要条件

### 8.3 后续发展

基于此框架，可进一步研究：
- **相互作用**: 通过世界面拓扑变化（管状图）
- **超弦**: 加入费米场，$c=10$
- **紧化**: 额外维度的几何反映在CFT中
- **对偶性**: 不同几何描述对应相同CFT

---

## 参考文献

- Polchinski, J. (1998). *String Theory, Vol. 1: An Introduction to the Bosonic String*. Cambridge University Press. Chapter 2.
- Becker, K., Becker, M., & Schwarz, J. H. (2007). *String Theory and M-Theory: A Modern Introduction*. Cambridge University Press.
- Di Francesco, P., Mathieu, P., & Sénéchal, D. (1997). *Conformal Field Theory*. Springer.
- Ginsparg, P. (1988). "Applied Conformal Field Theory." *arXiv:hep-th/9108028*.

---

*详细推导版，基于Polchinski弦论教材第二章*  
*包含完整数学推导与物理图像 🩷*